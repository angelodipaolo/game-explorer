# secrets
.env
*.env
.env.*
!.env.example

# databases
*.db
*.db-journal
*.db-shm
*.db-wal
/prisma/dev.db*
# your collection is private: the snapshot stays local (see data/README.md)
/data/*.db
/data/snapshot.json
/data/*.private.json

# dependencies
node_modules/

# next.js
.next/
out/
build/
next-env.d.ts

# tests
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
coverage/

# misc
.DS_Store
*.pem
*.tsbuildinfo
npm-debug.log*
.vercel
/scratch/
/prisma/test.db*
/.claude/settings.local.json
/.sabin
/.playwright-mcp/
*.png
!public/**/*.png
!/src/app/icon.png
!/src/app/apple-icon.png

# cached IGDB cover art and screenshots (npm run images:warm rebuilds it)
/.cache/

# map images
/data/maps/
/scripts/scratch/

# local backups (npm run backup)
/backups/

# journal photos
/data/journal/

# manual page scans
/data/manuals/

# the owner's own soundtrack files, one per MusicTrack id (GAMEEXPLOR-0025).
# Never committed, never under public/ — they are personal files, served only
# through /api/music/:trackId/audio.
/data/music/
